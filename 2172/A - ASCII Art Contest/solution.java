import java.util.Arrays;import java.util.Scanner; public class ASCIIArtContest {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);         // Read three scores        int a = sc.nextInt();        int b = sc.nextInt();        int c = sc.nextInt();         // Store in array and sort        int[] arr = {a, b, c};        Arrays.sort(arr);         // Check difference between max and min        if (arr[2] - arr[0] >= 10) {            System.out.println("check again");        } else {            System.out.println("final " + arr[1]); // median        }        sc.close();    }}